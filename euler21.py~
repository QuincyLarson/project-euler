n = []
m = []
o = []
p = []

#for i in xrange (2, 10000):
for j in xrange (2, 10000):
    for k in xrange (2, j): # OK so set up the loops
        if j % k == 0: # validate if it's a proper divisor
            m.append(k) # add to list for 
            if sum(m) == j:
                n.append(j)
                print n
            m = []












#                if sum(m) == i:
#                    for l in range (1, i):
#                        if i % l == 0:
#                            n.append(l)
#                    if sum(n) == j:
#                        p.append(i, j)
#        m = []
#        n = []
#            for k in xrange (1, i):
#                if i % k == 0:
#                    n.append(k)
#            if sum(n) == j:
#                p.append(m)
#                p.append(n)
#                print n, m


